This notebook follows along with the following video:   

[R - kNN - k nearest neighbor (part 1) ](https://www.youtube.com/watch?v=GtgJEVxl7DY&list=PLjPbBibKHH18I0mDb_H4uP3egypHIsvMn&index=22)

```{r}
data(iris)
```

```{r}
str(iris)
```

```{r}
table(iris$Species)
```

```{r}
head(iris)
```

```{r}
set.seed(9850)
```

```{r}
runif(5)
```

```{r}
gp<-runif(nrow(iris))
```

```{r}
head(gp)
```

```{r}
iris <- iris[order(gp),]
```

```{r}
str(iris)
```

```{r}
head(iris)
```

```{r}
head(iris,10)
```

```{r}
str(iris)
```

```{r}
summary(iris[,c(1,2,3,4)])
```

```{r}
normalize <- function(X) {
return ( (X-min(X)) / (max(X)-min(X)) )
}
```

```{r}
normalize(c(1,2,3,4,5))
```

```{r}
normalize(c(10,20,30,40,50))
```

```{r}
iris_n <- as.data.frame(lapply(iris[,c(1,2,3,4)], normalize))
```

```{r}
str(iris_n)
```

```{r}
summary(iris_n)
```
