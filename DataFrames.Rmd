
# Dataframes 

Data frames are the main object in R that allow one to do with a set of data. They are a lot like spreadsheets, where each row represents a record and the columns represent the different features that the data has.

```{r}
print("Hello")
```
